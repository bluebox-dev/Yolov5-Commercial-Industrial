# -*- coding: utf-8 -*-
"""
config.py — ค่าที่ปรับได้ทั้งหมดของ Workshop #2 อยู่ในไฟล์นี้ไฟล์เดียว

ไฟล์นี้ไม่ต้องกด Run — มันไม่ทำอะไรเอง เป็นแค่ที่เก็บตัวเลข
ไฟล์ step1 ถึง step6 จะอ่านค่าจากที่นี่ทั้งหมด

แก้ตัวเลขในไฟล์นี้แล้ว save → กด Run ไฟล์ step ไหนก็ได้ ค่าใหม่มีผลทันที
ไม่ต้องไล่แก้ทีละไฟล์ และห้ามก๊อปตาราง COLORS ไปวางไว้ที่อื่น
"""

# ─────────────────────────────────────────────────────────────────
#  1. ภาพที่ใช้ทดสอบ
# ─────────────────────────────────────────────────────────────────

# path นับจากโฟลเดอร์ Pylon+Opencv (โฟลเดอร์ที่ไฟล์นี้อยู่) จะใส่ path เต็มก็ได้
IMAGE_PATH = "../Candy.png"

# ย่อภาพให้กว้างเท่านี้ก่อนประมวลผลเสมอ
# ต้องย่อทุกภาพให้เท่ากันก่อน ค่า MIN_AREA ข้างล่างถึงจะใช้ได้กับทุกภาพ
WORK_WIDTH = 1200


# ─────────────────────────────────────────────────────────────────
#  2. เกณฑ์การนับชิ้นงาน
# ─────────────────────────────────────────────────────────────────

MIN_AREA = 1200      # เล็กกว่านี้ถือว่าเป็นเศษ ไม่นับ (หน่วยพิกเซล²)
MAX_AREA = 300000    # ใหญ่กว่านี้ถือว่าจับพื้นหลังผิด ไม่นับ
LONG_RATIO = 1.6     # ด้านยาว ÷ ด้านสั้น เกินค่านี้ = ทรงยาว, ไม่เกิน = ทรงกลม

OPEN_SIZE = 5        # ลบจุดรบกวนเม็ดเล็ก — ใหญ่ขึ้น = ลบแรงขึ้น
CLOSE_SIZE = 15      # อุดรูที่แสงสะท้อนเจาะกลางชิ้นงาน — ใหญ่ขึ้น = อุดแรงขึ้น
                     # ถ้าลูกอมที่วางชิดกันถูกนับรวมเป็นเม็ดเดียว ให้ลดค่านี้ลง


# ─────────────────────────────────────────────────────────────────
#  3. ตารางสีของลูกอมทั้ง 8 ชนิด  ←  หัวใจของ Workshop นี้
# ─────────────────────────────────────────────────────────────────
#
#   h = เฉดสี    0–179   ถ้าใส่ค่าแรกมากกว่าค่าหลัง = ช่วงคร่อมเลข 0 (โซนสีแดง)
#   s = ความสด   0–255   ยิ่งน้อยยิ่งซีดเข้าหาสีเทา
#   v = ความสว่าง 0–255
#   box = สีกรอบที่จะวาด เรียงเป็น (B, G, R) ไม่ใช่ (R, G, B)
#   th  = ชื่อไทยที่จะพิมพ์ในตารางสรุป
#
# ค่าชุดนี้วัดมาจากไฟล์ Candy.png จริง ใช้เป็นจุดตั้งต้นเท่านั้น
# เปลี่ยนไฟในห้องเมื่อไหร่ ต้องหาค่าใหม่ทุกครั้ง — ใช้แถบเลื่อนใน step3_extract.py
#
# จุดที่ต้องสังเกต: Halls-Pink, Skittles-Red และ Skittles-Black
# มีเฉดสี (h) เกือบเท่ากันหมด แยกกันได้ด้วยความสว่าง (v) เท่านั้น
COLORS = {
    #  ชื่อชนิด            h (เฉดสี)      s (ความสด)    v (ความสว่าง)   box = สีกรอบ B,G,R
    "Clorets-Green":   dict(h=(55, 78),   s=(90, 255),  v=(80, 255),  box=(60, 210, 60),   th="เขียว ทรงยาว"),
    "Clorets-Blue":    dict(h=(82, 100),  s=(90, 255),  v=(80, 255),  box=(215, 190, 60),  th="ฟ้า ทรงยาว"),
    "Halls-Purple":    dict(h=(105, 138), s=(40, 145),  v=(90, 205),  box=(200, 90, 140),  th="ม่วง"),
    "Halls-Pink":      dict(h=(163, 179), s=(110, 255), v=(150, 255), box=(140, 80, 255),  th="ชมพู"),
    "Skittles-Red":    dict(h=(163, 179), s=(110, 255), v=(92, 149),  box=(50, 50, 190),   th="แดงเข้ม"),
    "Skittles-Orange": dict(h=(3, 14),    s=(150, 255), v=(150, 255), box=(40, 150, 255),  th="ส้ม"),
    "Skittles-Yellow": dict(h=(18, 33),   s=(150, 255), v=(160, 255), box=(60, 215, 235),  th="เหลือง"),
    # เม็ดนี้เข้มจนค่าเฉดสีไม่น่าเชื่อถือ จึงจับด้วย "ความมืด" แทน (เปิด h ไว้ทั้งวง)
    "Skittles-Black":  dict(h=(0, 179),   s=(35, 255),  v=(0, 91),    box=(90, 90, 90),    th="ดำ/น้ำตาล"),
}

# ชนิดที่ step2, step3 และ step4 จะหยิบมาทำทีละชนิด
# อยากดูชนิดอื่น เปลี่ยนชื่อในบรรทัดนี้ให้ตรงกับชื่อในตาราง COLORS ข้างบน
ONE_CLASS = "Clorets-Green"


# ─────────────────────────────────────────────────────────────────
#  4. ค่ากล้อง Basler  (ใช้เฉพาะ step1 โหมดกล้อง และ step6)
# ─────────────────────────────────────────────────────────────────

# ไฟล์ค่ากล้องที่เซฟจาก pylon Viewer ตอน Workshop #1
# ถ้าไม่มีไฟล์นี้ โปรแกรมจะใช้ค่า EXPOSURE_US และ GAIN_DB ข้างล่างแทน
PFS_FILE = "Base-Conf.pfs"

EXPOSURE_US = 8000.0   # เวลารับแสง หน่วยไมโครวินาที
GAIN_DB = 0.0          # เพิ่มเป็นทางเลือกสุดท้าย — เพิ่มไฟก่อนเสมอ
