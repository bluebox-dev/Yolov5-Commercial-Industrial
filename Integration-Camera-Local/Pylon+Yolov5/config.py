# -*- coding: utf-8 -*-
"""
config.py — ค่าที่ปรับได้ทั้งหมดของ Workshop #3 อยู่ในไฟล์นี้ไฟล์เดียว

ไฟล์นี้ไม่ต้องกด Run — มันไม่ทำอะไรเอง เป็นแค่ที่เก็บตัวเลข
ไฟล์ step1 ถึง step4 จะอ่านค่าจากที่นี่ทั้งหมด

โครงเดียวกับ config.py ของ Workshop #2 — แก้ที่นี่ที่เดียว มีผลทุกไฟล์
"""

# ─────────────────────────────────────────────────────────────────
#  1. โมเดล
# ─────────────────────────────────────────────────────────────────

# ไฟล์ weight ที่เทรนมาจาก Colab — วางอยู่ในโฟลเดอร์เดียวกับไฟล์นี้แล้ว
MODEL_PATH = "best.pt"

CONF = 0.40    # ความมั่นใจขั้นต่ำที่จะยอมรับว่าเจอ
               #   ต่ำลง → เจอเยอะขึ้น แต่มีของปลอมปนมากขึ้น (false positive)
               #   สูงขึ้น → เอาเฉพาะที่มั่นใจ แต่อาจพลาดของจริง (false negative)

IOU = 0.45     # เกณฑ์รวมกรอบที่ทับกัน (NMS) — ต่ำลง = ตัดกรอบซ้ำแรงขึ้น

IMGSZ = 640    # ขนาดภาพที่ป้อนเข้าโมเดล ควรเท่ากับ --img ตอนเทรน
               # ลดเป็น 416 หรือ 320 จะเร็วขึ้นชัดเจน แลกกับความแม่นของวัตถุเล็ก


# ตัวประมวลผลที่จะใช้ — ปล่อย "auto" ไว้ได้เลย ไม่ต้องแก้
#   Mac ชิป M-series       → ได้ mps  (GPU ในตัว)
#   Windows + การ์ด NVIDIA → ได้ cuda (เร็วที่สุด)
#   Windows ไม่มีการ์ดจอ    → ได้ cpu  (ยังทำงานได้ปกติ)
# อยากบังคับเอง ใส่ได้: "cuda" · "mps" · "cpu" — ผลลัพธ์เหมือนกัน ต่างแค่ความเร็ว
DEVICE = "auto"


# ─────────────────────────────────────────────────────────────────
#  2. ไฟล์ต้นทาง  ←  จุดที่แก้บ่อยที่สุด
# ─────────────────────────────────────────────────────────────────
#
# path นับจากโฟลเดอร์ Pylon+Yolov5 (โฟลเดอร์ที่ไฟล์นี้อยู่) จะใส่ path เต็มก็ได้

# step2_image.py อ่านจากบรรทัดนี้ — ภาพเดียวกับที่ใช้ใน Workshop #2
IMAGE_SOURCE = "../Candy.png"

# step3_video.py อ่านจากบรรทัดนี้
# วิดีโอที่มีให้เลือกในโปรเจกต์:  1.mp4 · 2.mp4 · 3.mp4 · homework.mp4
VIDEO_SOURCE = "../../Dataset/Video_Origin/1.mp4"

WORK_WIDTH = 1200    # ย่อภาพก่อนประมวลผล ให้พอดีจอและเร็วขึ้น


# ─────────────────────────────────────────────────────────────────
#  3. ค่าที่ใช้เฉพาะโหมดวิดีโอ (step3)
# ─────────────────────────────────────────────────────────────────

LOOP_VIDEO = False     # เล่นจบแล้ววนกลับไปเริ่มใหม่
FRAME_STRIDE = 1      # ประมวลผลทุกกี่เฟรม — ใส่ 2 หรือ 3 ถ้าเครื่องช้า จะลื่นขึ้น
SAVE_VIDEO = False    # True = เซฟวิดีโอที่วาดกรอบแล้วเป็น result.mp4 ด้วย


# ─────────────────────────────────────────────────────────────────
#  4. ค่ากล้อง Basler (ใช้เฉพาะ step4)
# ─────────────────────────────────────────────────────────────────

PFS_FILE = "Base-Conf.pfs"   # ไฟล์ค่ากล้องที่เซฟจาก pylon Viewer ตอน Workshop #1
EXPOSURE_US = 8000.0
GAIN_DB = 0.0


# ─────────────────────────────────────────────────────────────────
#  5. สีกรอบประจำแต่ละชนิด  (B, G, R)
# ─────────────────────────────────────────────────────────────────
#
# ใช้ชุดเดียวกับ Workshop #2 เพื่อให้เทียบผลสองวิธีได้ด้วยตาทันที
# ชนิดที่ไม่มีในตารางนี้จะถูกวาดด้วยสีเขียวเป็นค่าสำรอง
BOX = {
    "Clorets-Green":   (60, 210, 60),
    "Clorets-Blue":    (215, 190, 60),
    "Halls-Purple":    (200, 90, 140),
    "Halls-Pink":      (140, 80, 255),
    "Skittles-Red":    (50, 50, 190),
    "Skittles-Orange": (40, 150, 255),
    "Skittles-Yellow": (60, 215, 235),
    "Skittles-Black":  (90, 90, 90),
}
